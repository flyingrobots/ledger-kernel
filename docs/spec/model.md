---
title: State‑Transition Model
---

# State‑Transition Model

This page mirrors the formal model from `MODEL.md`. The canonical source lives at the repository root; the docs build will sync it here.

> Temporary: until the sync is enabled, read the full text in `MODEL.md` at the repo root.
